from setuptools import setup, find_packages

setup(
    name="tools",
    version="1.0.0",
    description="GeoSlicer tool scripts",
    author="""LTrace Team (LTrace Geophysics)""",
    packages=find_packages(),
    include_package_data=True,
)
